{
  "name": "bottle-market",
  "version": "2.0.2",
  "private": true,
  "author": "RedQ, Inc.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna clean --yes && rimraf node_modules",
    "clean:build": "lerna exec -- rimraf \"{.next,dist,build}\"",
    "dev:shop": "lerna run --parallel shop-dev",
    "build:shop": "yarn workspace @bottle-market/shop build",
    "serve:shop": "yarn  workspace @bottle-market/shop serve",
    "dev:admin": "concurrently \"yarn workspace @bottle-market/admin start\" \"yarn workspace @bottle-market/api admin\"",
    "build:admin": "yarn workspace @bottle-market/admin build",
    "serve:admin": "yarn  workspace @bottle-market/admin serve",
    "build:api": "yarn workspace @bottle-market/api build",
    "dev:api-shop": "yarn workspace @bottle-market/api shop-dev",
    "serve:api-shop": "yarn workspace @bottle-market/api shop-serve",
    "dev:api-admin": "yarn workspace @bottle-market/api admin"
  },
  "devDependencies": {
    "cross-env": "^7.0.2",
    "husky": "^4.2.3",
    "lerna": "^3.20.2",
    "lint-staged": "^10.1.2",
    "prettier": "^2.0.4",
    "rimraf": "^3.0.2",
    "concurrently": "^5.1.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,md,css}": [
      "prettier --trailing-comma es5 --single-quote --write"
    ]
  },
  "dependencies": {
    "rxjs": "^6.5.5",
    "rxjs-compat": "^6.5.5"
  }
}
