CREATE TABLE delivery_area (
  id uuid primary key,
  store_id uuid references store (id),
  name varchar(50),
  address varchar(255),
  geom geometry(POINT,4326),
  radius int,
  monday bool,
  tuesday bool,
  wednesday bool,
  thursday bool,
  friday bool,
  saturday bool,
  sunday bool,
  monday_hours_from int,
  monday_hours_to int,
  tuesday_hours_from int,
  tuesday_hours_to int,
  wednesday_hours_from int,
  wednesday_hours_to int,
  thursday_hours_from int,
  thursday_hours_to int,
  friday_hours_from int,
  friday_hours_to int,
  saturday_hours_from int,
  saturday_hours_to int,
  sunday_hours_from int,
  sunday_hours_to int,  
  date_added timestamp
);

CREATE INDEX idx_deliver_area_geom ON delivery_area USING gist(geom);  
