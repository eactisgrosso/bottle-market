CREATE TABLE events (aggregateId BINARY(16), aggregateType VARCHAR(255), sequence INT, eventType VARCHAR(255), eventData json, userId BINARY(16), timestamp DATETIME, PRIMARY KEY(aggregateId, sequence));
